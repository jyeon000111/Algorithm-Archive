## 1. 문제 정보 (Problem)

  - **문제 이름**: 5658. [모의 SW 역량테스트] 보물상자 비밀번호
  - **출처 및 번호**: [SWEA 5658번]
  - **난이도**: 
  - **문제 요약**: 비밀번호(보물상자에 적힌 숫자로 만들 수 있는 모든 수 중 K번째로 큰 수를 10진법으로 변환하여 출력)

---
## 2. 핵심 아이디어 (Core Logic)
- 한 변에 N/4개의 숫자 배치. N/4 회전 돌리면 처음과 같음.
- 숫자배열을 두 번 반복한 문자열 생성해서 슬라이싱으로 모든 수 뽑아내기
- set에 담아 중복 제거
- int('문자열', 진법수) 로 10진수로 변환 후, 정렬하여 K번째로 큰 수 구하기

---
## 4. 새롭게 배운 점 (What I Learned)
- 16진수를 10진수로 변환하는 방법
```python
hex_string = "1A"
decimal_number = int(hex_string, 16)
print(decimal_number)  # 출력: 26
```

- lambda 함수 (map 함수와 사용)
`map(lambda x: x 표현식, list)`

